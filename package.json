{
  "name": "maplibre-gl-shortbread-styler",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "compile": "rollup -c",
    "build": "rm -rf dist && npm run compile",
    "lint": "eslint",
    "watch": "watch 'npm run compile' src"
  },
  "devDependencies": {
    "@rollup/plugin-typescript": "^11.1.6",
    "eslint": "^8.57.0",
    "http-server": "^14.1.1",
    "rollup": "^4.12.0",
    "tslib": "^2.6.2",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@versatiles/style": "^4.2.2",
    "maplibre-gl": "^4.1.0"
  }
}
